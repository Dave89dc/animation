import { ButtonResizeDirective } from './button-resize.directive';

describe('ButtonResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonResizeDirective();
    expect(directive).toBeTruthy();
  });
});
